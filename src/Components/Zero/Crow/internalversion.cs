using System;
using System.Diagnostics;

namespace src
{
    class internalversion // This class is used for reading the VersionOverview.md, whicb contains the version codename and the version number.When running the --i v command, Zero will awake this sub-part
    {
        public void readversionoverview(string signal)
        {
            
        }
    }
}