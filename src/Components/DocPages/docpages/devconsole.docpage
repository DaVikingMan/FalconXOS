About Dev Console:
                 A console or terminal-like prompt designed for debugging and executing specific commands which can't be executed in User-friendly Terminal.

Commands listed:
     
      write::input - for writing input manually to temp inputstreams
      exit - For exiting devconsole.Going back to startfi.
      --open in - For reading(opening)files which are in the FalconXOS/ directory.
      --open exit - For reading(opening)files which are not in the FalconXOS/ directory.
      config - For opening the config console.This is a feature which is currently in alpha builds and is not complete.

Other usages:
           Can also be used for creating custom commands.
 
Documented by:
             DaVikingMan, author and creator of FalconXOS
